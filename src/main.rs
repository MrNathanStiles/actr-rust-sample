use std::ffi::c_char;

use actr::_actr_log_length;

mod actr;

fn main() {
    
}
fn actr_step(delta: f32) {
    let _message = "hello world";
    let message = &raw const _message;
    unsafe  {

        _actr_log_length(message, _message.len());

    }
 
}
